<% if (process === 'checkreg' && rownum === 1) { %>
<input type="hidden" id="hiddenstatus" value="<%=rownum%>" />
<% } %>
<% if (process === 'checklogin') { %>
<input type="hidden" id="hiddenstatus" value="<%=loginstatus%>" />
<% } %>